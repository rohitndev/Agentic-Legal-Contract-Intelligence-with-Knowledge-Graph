# Documentation Assets

This folder holds the images referenced by the root [`README.md`](../README.md).

| File | How to produce it |
| --- | --- |
| `demo-terminal.png` | Screenshot of `python -m scripts.run_demo` output |
| `api-docs.png` | Screenshot of `http://localhost:8000/docs` (Swagger UI) |

All other `*.png` files are ChatGPT-generated diagrams (white background, clean
icons) — one prompt per image.
